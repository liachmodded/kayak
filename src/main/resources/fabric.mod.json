{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "kayak",
  "description": "${description}",
  "authors": [
    "liach"
  ],
  "contact": {
    "homepage": "https://github.com/${repoOwner}/${repoName}",
    "sources": "https://github.com/${repoOwner}/${repoName}"
  },

  "license": "MPL-2.0",
  "icon": "assets/${id}/icon.png",

  "environment": "*",
  "entrypoints": {
    "client": [
      "com.github.liachmodded.kayak.client.KayakClient"
    ],
    "main": [
      "com.github.liachmodded.kayak.Kayak"
    ]
  },
  "mixins": [
    "kayak.mixins.json"
  ],

  "depends": {
    "fabricloader": "${loaderVersionLimit}",
    "minecraft": "${minecraftVersionLimit}"
  }
}
